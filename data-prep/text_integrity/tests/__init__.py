"""Unit tests for the text-integrity audit tool.

Run with:
    cd data-prep && python3 -m unittest discover text_integrity/tests -v

These are pure-Python tests against synthetic TEI XML fixtures — they do not
need a built database and don't touch data-sources/.
"""
