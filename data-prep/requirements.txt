# Data preparation dependencies
lxml>=6.0.3
tqdm>=4.67.3
click>=8.3.2
colorama>=0.4.6

# CLTK for Greek NLP (dependency parsing, POS tagging)
# Note: CLTK will download Greek models (~2GB) on first use
# To pre-download: python -c "from cltk import NLP; NLP(language_code='grc')"
cltk>=2.5.1

# Sanskrit transliteration (IAST to Devanagari)
# Required for Sanskrit database creation - all Sanskrit text must be in Devanagari
indic-transliteration>=2.3.82
